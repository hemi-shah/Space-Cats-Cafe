using UnityEngine;

public class TicketBoard : MonoBehaviour
{
    /*
    // All ChatGPT
    
    [Header("Prefab & Parent")]
    [SerializeField] private OrderTicket ticketPrefab;
    [SerializeField] private RectTransform ticketsParent;

    public OrderTicket SpawnTicket(string drinkName, Sprite drinkIcon)
    {
        if (!ticketPrefab || !ticketsParent) return null;
        
        var ticket = Instantiate(ticketPrefab, ticketsParent);
        ticket.transform.SetAsLastSibling();

        ticket.Setup(drinkName, drinkIcon);
        
        return ticket;
    }
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
