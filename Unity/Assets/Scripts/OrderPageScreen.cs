using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class OrderPageScreen : MonoBehaviour
{

    /*
    [Header("Customer References")] 
    [SerializeField] private string[] customerIds;
    [SerializeField] private GameObject[] customerCats;
    [SerializeField] private Button[] takeOrderButtons;
    [SerializeField] private Sprite[] customerSprites;

    [Header("Navigation")] [SerializeField]
    private GameObject takeOrderScreenRoot;
    
    */
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
